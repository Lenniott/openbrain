version: "3.9"

services:
  openbrain-api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: openbrain-api
    ports:
      - "${APP_PORT:-7788}:8000"
    networks:
      - n8n_net
    restart: unless-stopped

networks:
  n8n_net:
    external: true

